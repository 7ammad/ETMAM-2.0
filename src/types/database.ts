/**
 * Supabase-generated types (run: npx supabase gen types typescript).
 * Placeholder until migrations are run.
 */
export type Json = string | number | boolean | null | Json[] | { [key: string]: Json };
